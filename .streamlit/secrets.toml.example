# Streamlit Secrets Configuration (Example)
# Copy this to secrets.toml and fill in your values
# NEVER commit secrets.toml to git!

# Telegram Bot Configuration (Optional)
# Get your bot token from @BotFather on Telegram
# Get your chat ID from @userinfobot
[telegram]
bot_token = "YOUR_BOT_TOKEN_HERE"
chat_id = "YOUR_CHAT_ID_HERE"

# Database Configuration (Optional - for Postgres)
# For production, consider using a hosted database like Neon or Supabase
[database]
# url = "postgresql://user:password@host:port/database"

# API Keys (if you add premium data sources later)
# [api_keys]
# alpha_vantage = "YOUR_KEY"
# finnhub = "YOUR_KEY"
